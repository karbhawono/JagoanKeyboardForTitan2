# Project-wide Gradle settings.

# Enable AndroidX
android.useAndroidX=true

# Disable Jetifier (we're using AndroidX only)
android.enableJetifier=false

# Enable R8 full mode for better optimization
android.enableR8.fullMode=true

# Enable non-transitive R classes for faster builds
android.nonTransitiveRClass=true

# Suppress unsupported compileSdk warning
android.suppressUnsupportedCompileSdk=35

# Enable configuration cache for faster builds
# Disabled temporarily due to incrementBuildNumber task incompatibility
org.gradle.configuration-cache=false

# Increase JVM heap size for better build performance
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# Enable Gradle caching
org.gradle.caching=true

# Enable parallel execution
org.gradle.parallel=true

# Kotlin code generation
kotlin.code.style=official
